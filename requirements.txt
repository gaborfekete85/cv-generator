# Core web + template stack (pure-Python, no system deps required)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
jinja2>=3.1
markdown>=3.6
pyyaml>=6.0
httpx>=0.27
beautifulsoup4>=4.12

# Clerk auth — verifies session JWTs against Clerk's JWKS endpoint.
# [crypto] pulls in the RSA signature backend; `cryptography` is already
# present transitively via xhtml2pdf, so no extra C deps here.
pyjwt[crypto]>=2.8

# Auto-load .env on startup. Without this, plain `uvicorn backend.main:app`
# ignores the file and CLERK_* shows up unset, which shows up as
# "Authentication is not configured" on the login page.
python-dotenv>=1.0

# Required by FastAPI's UploadFile / multipart form parsing — used by the
# profile-picture upload endpoint.
python-multipart>=0.0.9

# Optional — enables the "Semantic similarity" match strategy.
# Uses ONNX-runtime + a ~22MB sentence-embedding model (downloaded on first
# use). Adds roughly 100MB to the installed footprint. Leave commented out
# if you only need the default keyword matcher; the API gracefully
# advertises the strategy as "unavailable" when this isn't installed.
fastembed>=0.3


# QR code + user icon image generation (Pillow is pulled in by qrcode[pil])
qrcode[pil]>=7.4

# PDF rendering — pure-Python. Works everywhere pip works, no system libs.
# This is now the ONLY backend the Docker image ships with, matching the
# default macOS dev setup (where WeasyPrint would silently fall back to
# xhtml2pdf anyway because Pango is usually not installed on a Mac).
xhtml2pdf>=0.2.15

# WeasyPrint was previously the preferred backend when Pango/Cairo native
# libs were available. It gives nicer output (gradients, text-transform,
# absolute positioning) but produces a CV that looks different from the
# xhtml2pdf fallback. Uncomment the two lines below — and the matching
# apt packages in the Dockerfile — if you specifically want WeasyPrint.
# weasyprint>=62.0; sys_platform != "win32"
